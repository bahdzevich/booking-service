server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/BOOKING_SERVICE
    username: postgres
    password: WhyTryHarder
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: true